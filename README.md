# Week 3 – Contact Management System
This is my Week 3 project for **The Developers Arena** internship.  
In this project, I built a complete Contact Management System using **functions, dictionaries, file handling, JSON, CSV**, and **error handling** in Python.

---

## 📌 Project Overview
The Contact Management System allows users to:

- Add new contacts
- Search contacts (partial matching supported)
- Update existing contacts
- Delete contacts
- View all contacts in a formatted style
- Save data automatically to a JSON file
- Load contacts when the program starts
- Export contacts to CSV
- View contact statistics (groups, totals)

This project demonstrates the use of **functions, dictionaries, regex validation, JSON persistence, CSV exporting**, and a complete **menu-driven interface**.

---

## 📚 What I Learned
- How to create and use **functions** effectively  
- Using **dictionaries** for structured data  
- Validating input (phone numbers, emails)  
- **Regex** for phone and email validation  
- **File operations** using JSON  
- **Exporting data** to CSV  
- Error handling using **try / except**  
- Building a clean **menu-driven application**  
- Organizing programs into multiple small, reusable functions  

---

## 🧱 Project Structure

week3-contact-manager/
│── contacts_manager.py # Main program
│── contacts_data.json # Auto-generated contacts database
│── test_contacts.py # (Optional) test file
│── README.md # Documentation
│── requirements.txt # (Optional) dependencies
└── .gitignore # Ignore unnecessary files